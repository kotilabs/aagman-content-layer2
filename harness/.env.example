# ─────────────────────────────────────────────────────────────
# AAGMAN HARNESS — environment template
# Copy to .env and fill real values.  NEVER commit .env.
# ─────────────────────────────────────────────────────────────

# ── LLM providers ──
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-proj-...
GOOGLE_API_KEY=...

# ── Telegram (human gates + alerts). Stub-safe: harness prints if unset. ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── SEBI compliance identity (ComplianceGate REQUIRED_STRINGS) ──
# Public registration details — not secrets.
AAGMAN_RIA_NAME=Koti Labs
AAGMAN_SEBI_REG_NUMBER=INA000021951

# ── Budget guard (harness_core/budget.py) ──
DAILY_BUDGET_USD=5.00
