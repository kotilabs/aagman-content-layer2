current_phase: 7 (LIVE — partially proven; awaiting one more iteration + budget reset)
last_completed: phase-7 fix @ 7a2af81 — panel verdict parsing (approvals misread as blocks), found+fixed live; 178 tests green
blocked: no hard blocker — key now valid; remaining = a clean PUBLISH (content had a real CTA flaw the judge caught) + budget reset (spent $4.53/$5 today)
notes:
  - PHASE 7 LIVE (2026-07-03): Anthropic key wired from ~/Documents/aagman-v2/.env (plaintext, VALID for claude-sonnet-4-6) into harness .env (gitignored, never printed/committed). export the 2 keys inline before run (run.py load_env doesn't push to os.environ; source .env breaks on `AAGMAN_RIA_NAME=Koti Labs` space).
  - LIVE CONTENT PIPELINE PROVEN end-to-end through ALL panels: ideate → create(text+image, disclosure) → COMPLIANCE PANEL PASSES (sonnet+opus) → JUDGE PANEL → escalate-on-split (Opus blocked a real CTA/engagement-bait in the image storyboard Card 05; article body passed) → parked at judge for human. True-positive quality gating. NO published artifact yet BECAUSE the content genuinely had a CTA flaw + budget near cap.
  - CRITICAL BUG found+fixed live (7a2af81, TDD): JudgmentPanel._default_parse scanned the WHOLE reply for block|fail|reject|violat, so every compliance approval ('does not violate / nothing to reject') was misread as BLOCK → gate could never pass at threshold 1.0 → burned $2.71 of Opus on pointless revises before parking. Fix: read the STATED verdict (structured 'VERDICT: pass|block' line now demanded by the vote prompt, or the leading token), never the rationale body. This was THE blocker to a live publish.
  - COST: $4.53/$5 today (mostly Opus panels; $2.71 wasted pre-fix). Budget guard NOT tripped. Another full run (~$1.8) would breach → wait for reset or raise DAILY_BUDGET_USD.
  - DESIGN GAP noted (not fixed): a judge-AGENT escalate parks with no human APPROVED/REJECTED path (only HumanGate has that). To resolve a judge-escalate you re-run or intervene. Worth routing agent-escalate to a HumanGate-like approval later.
  - RECURRENCE -> ARCHITECTURAL-REVIEW (built @ this commit, 185 tests): FixMemory logs each fix outcome to engineering/fixlog tagged by area (_area_of = 2-seg path of entry_file, else layer); RCAAgent at ideate reads area history, and if total>=3 OR adverse(reopened/bounced)>=2 sets plan['architectural_review']=True + "⚠️ ARCHITECTURAL REVIEW RECOMMENDED" banner in summary + feeds recurrence into the RCA prompt -> surfaces at HumanGate('plan') for the human to decide patch-vs-rearchitect. Answers "when does the harness flag architecture over patchwork". v1: count-based (no time window), unknown areas never flagged. Demoed: 3 fixes to backend/src -> the 4th variation flagged.
  - REMAINING Phase-7: (1) clean PUBLISH — revise storyboard to drop the CTA (or cleaner brief) + budget; (2) engineering RCA live (key now valid — RCA will run via sonnet/opus); (3) attribute() scoring activation; (4) kill-switch live drill. run.py --domain content --once with both gates pre-approved at gates/content:phase7_brief.md/{brief,publish}/APPROVED.
  - phase 8: HARNESS_STATUS.md written (what runs, costs, blockers, Phase-7 remaining, Phase-2 backlog, how-to-run). Build committed through phase 6 (7 commits, phase 0-6). NO PUSH (no git remote; user rule = never push unless asked). Telegram pinged with the pause + blocker. RESUME POINT for Phase 7: valid Anthropic key in .env -> `run.py --domain content --once` (approve brief+publish) + `--domain engineering` (approve plan) -> one real artifact + one real PR + kill drill + attribute() scoring.
  - phase 6: extensibility + genericity PROVEN. harness_configs/distribution_config.py = a THIRD domain (stub agents on all 6 ABCs) that plugs in with ZERO harness_core changes -> `--domain distribution` loads. VERIFY green: 173 tests; genericity grep CLEAN (no sebi/linkedin/github/content in harness_core/*.py beyond benign litellm role/content); single-source grep CLEAN (no model strings in harness_core/*.py — only models.yaml); all three domains (content/engineering/distribution) load through the same GenericHarness with 6 steps each. The same ComplianceGate class serves SEBI_RULES + RISK_RULES, differing only by rules file.
  - phase 5: ENGINEERING domain COMPLETE (built + tests green + live sense/fault-isolation proven). 6 agents (harness_engineering/agents.py): IssueListener (gh issues assignee ajitk2003, fetch injected), RCAAgent (four laws + layer_ground_truth routing + file_relationships + rca_panel + second pass on confidence<0.7), CodeFixAgent (Claude Code subprocess via sys.executable temp script, test-first framing, runner injected), AdversarialReview (rca_panel loaded with adversarial_vocab, drives create.revise loop_limit 2), PRAgent (branch ajit/fix-{n}, assign amit-kotidev, pr_fn injected), FixMemory (outcomes -> engineering ideate/judge namespaces). harness_engineering/gates.py: CriticalApprovalGate (extra approve_critical sign-off ONLY for p0/ship-blocker/critical issues). harness_configs/engineering_config.py wires the test harness as TWO TestGates (Decision 6) with REAL discovered commands: create=`run_all.sh --fast` fixable (inner loop), judge=`run_all.sh` full escalate (outer net). ComplianceGate(RISK_RULES) on rca_panel (override-bleed-safe). 170 tests green (was 148).
  - TEST_HARNESS.md written from a READ-ONLY inventory of ~/Documents/aagman-v2 (real commands verbatim): full=`bash regression-testing/run_all.sh` (--fast skips integration; `run_all.sh <area>` per-area), single=`uv run --project backtester pytest <path> -v`, per-layer pnpm --filter <svc> run test / uv run pytest via dotenvx. 8 areas. risk-calculations.ts confirmed. NOTE: the spec's 22-check verify.sh / dev_lab does NOT EXIST in aagman-v2 — judge gate uses full run_all.sh instead (documented, not fabricated).
  - LIVE DRY RUN (real): IssueListener fetched 50 real open issues -> RCA attempted -> ALL blocked by Anthropic auth (override [opus,sonnet], no gpt-4o fallback) -> fault isolation marked each item 'failed' + audited the exact AuthenticationError -> loop finished CLEAN (no crash). Proves: live gh sense + dedup + per-item fault containment. Blocked downstream (plan/TestGates/PR) by (a) valid Anthropic key for RCA, (b) aagman-v2 test-env setup (uv/dotenvx/.env/DB) for the TestGates.
  - CORE robustness fix (TDD): run_item now CONTAINS an agent exception per-item (mark failed + audit 'error' + LEARN still runs), so one item's model outage never crashes the loop or its siblings. KILL/budget still halt via _stopped() (bool, not exception).
  - HANDOVER FLAG for Ajit: engineering.ideate override [opus,sonnet] + panels judge_panel/compliance_panel [sonnet,opus] are Anthropic-ONLY (no cross-provider fallback). Adding gpt-4o as a last fallback to these chains in models.yaml (one line each) gives cross-provider resilience AND lets the harness run live via gpt-4o when Anthropic is down. Left as-is (spec-authoritative); user's call.
  - phase 4: CONTENT domain COMPLETE (built + tests green + live wiring proven). 6 agents (harness_content/agents.py: ContentSensor briefs-inbox / ContentStrategist ideate / ContentCreator parallel text+image, every text opens with RIA disclosure / ContentJudge judge_panel drives loop_limit-2 revise / ContentPublisher outbox / ContentMemory learn). harness_configs/content_config.py wires gates: ideate=HumanGate(brief,24), create=ComplianceGate(SEBI_RULES)+parallel[text,image], judge loop_limit2, publish=HumanGate(publish,48). run.py: Services bundle now injects router+memory_factory+telegram into build_config(services) AND the harness (completed the Phase-1 "wired later" stub). 148 tests green (was 123).
  - LIVE DRY RUN (real, not mock): sense reads inbox brief -> ideate produces real on-brand educational content -> brief HumanGate fires+parks -> [approve] -> create fan-out text+image -> parks at compliance. PROVEN live: pipeline wiring, gate fire/park/resume, work-item state machine + audit, real LLM generation. Ledger proof in logs/cost_log.jsonl (gpt-4o only).
  - KEY STATE (RCA, proof via litellm probe): OPENAI key VALID (gpt-4o works); ANTHROPIC key INVALID here (auth error 'invalid x-api-key') -> ALL Claude 4.x models fail -> chains with a gpt-4o fallback survive (ideate/create/classification/rca_panel), but the Anthropic-only panels judge_panel + compliance_panel = [sonnet,opus] get ZERO votes. => full live content flow (create-compliance-pass -> judge-pass -> awaiting_publish) BLOCKED on a valid ANTHROPIC_API_KEY. This is a config/secret blocker, NOT a code blocker (offline unit tests prove those panels' logic). Phase 7 needs valid Anthropic (+Google) keys in .env.
  - TWO BUGS found via the live run + fixed TDD: (1) override-bleed — passing domain/step to a gate's JudgmentPanel let resolve() pick the content.create GENERATION override instead of the compliance_panel; gate panels now use pure panel chains. (2) gates._tally on NO votes returned block-fixable -> burned revise cycles on an infra/auth outage; now ESCALATES (absence of expected data -> human, matches JudgmentPanel._tally + RISK law a). Also fixed test-pollution: content test router wrote fake spend into the real logs/cost_log.jsonl budget ledger -> now cost_log=os.devnull in tests.
  - to drop a brief: put a .md/.txt/.json file in inbox/ (json may set {"brief","channel"}); outputs land in outbox/{channel}/. inbox/ outbox/ gates/ gitignored.
  - phase 3 @ c6dc0a2: harness_content/SEBI_RULES.md (6 base SEBI rules + 7 stricter content-layer2/product rules + REQUIRED_STRINGS=Koti Labs/INA000021951) and harness_engineering/RISK_RULES.md (Indian-market invariants + layer law + 4 engineering laws verbatim + branch law ajit/fix-{n} + regression law). ComplianceGate ENHANCED (TDD): deterministically enforces REQUIRED_STRINGS in code (mandatory RIA disclosure blocks independent of LLM panel) — TDD caught+fixed a parser bug ('---' hr misread as bullet). VERIFY: 123 tests green (was 109); 9 integration tests prove SAME ComplianceGate enforces both rules files (genericity end-to-end); grep clean. REQUIRED_STRINGS parse = bullets under '## REQUIRED_STRINGS' heading, marker must be followed by space.
  - phase 2 @ d80b0f3: distillation + Day-0 seeding COMPLETE. 7 engineering JSONs (from aagman-v2 issues/PRs/code + LOCAL non-git memories) under harness_engineering/distilled/ + report; content artifacts (BRAND_VOICE.md + examples15/rejected13/channel_rules6 + sebi_extras + report) under harness_content/ mined from content-layer2 + local compliance memories. seed_memory.py (flexible extractor -> AgentMemory, reproducible). VERIFY: 109 tests green; all 7 namespaces seed >0 via REAL AgentMemory API (eng sense206/ideate380/create37/judge39/gate10, content create15/judge13); top_k retrieval works on real data; key-shaped secret scan CLEAN. data/memory.db gitignored -> rebuild via `PYTHONPATH=. venv312/bin/python3 seed_memory.py`. HONEST FINDING (content subagent): published content-layer2 output carries NO standalone RIA/SEBI disclosure footer — compliance is structural (no buy/sell, open-question endings); harness ADDS explicit not_investment_advice disclosure as harness policy (Phase 3/4), flagged not claimed as existing practice.
  - phase 0 @ 531948c: SEPARATE repo ~/Documents/aagman-harness on main; venv312 (py3.12.13) + deps (litellm PINNED 1.85.0); secrets-safe .gitignore; harness_configs/models.yaml single-source (all chain models priced); local commits only (no git remote yet).
  - phase 1: harness_core COMPLETE, 106 tests green (TDD). Modules: gates (Gate/GateResult/TestGate/ComplianceGate/QualityGate/HumanGate, panel injected), agent_base (6 ABCs + Signal/Idea/Artifact/Verdict/Receipt; Artifact field is `body` not `content` for genericity), domain_config (StepConfig/GateConfig/DomainConfig, LOAD-TIME validation, recursive=False), llm_router (models.yaml single-source + startup pricing check + resolve overrides>panels>defaults + fallback + cost log + call_model + resolver stub), budget (daily guard auto-pause), kill_switch, telegram (stub-safe), state (WorkItemStore dedup/resume/audit + relied_lessons col), agent_memory (namespaced lessons + top_k + count + attribute + helped/misled/confidence self-evolution cols), judgment_panel (vote + escalate-on-split), loop_controller, generic_harness (SENSE→IDEATE→CREATE→JUDGE→PUBLISH→LEARN, fixable-retry, park, dedup, kill/budget checks, parallel fan-out), run.py + run.sh (CLI). VERIFY green: 106 tests + genericity (only litellm role/content API key remains) + single-source (no model strings outside models.yaml).
  - source of truth: AAGMAN_HARNESS_FINAL-5.md (repo root).
  - DIRECTIVE (Ajit): Phase 2/3 must ALSO mine LOCAL non-git session learnings — ~/.claude/projects/*/memory (170 in -Users-ajitkumar, 107 in aagman-v2) + ~/.claude/CLAUDE.md + aagman-v2 .planning/docs/CLAUDE.md/AGENTS.md — into adversarial_vocab/layer_ground_truth/RCA-seed/RISK_RULES. Skip secret lines.
  - gh authed (repo scope); aagman-v2 PRIVATE + aagman-content-layer2 PUBLIC visible -> Phase 2 UNBLOCKED. NO .env keys yet -> Phase 4/5 dry-runs + Phase 7 live need ANTHROPIC/OPENAI/GOOGLE keys (BLOCKED.md + Telegram 7047754123 when hit).
