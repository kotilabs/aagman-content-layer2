TASK: Generate exactly {{ count }} Google RSA headlines for this variant.
- Each headline must be {{ max_chars }} characters or fewer INCLUDING spaces and punctuation.
- Return ONLY a numbered list, one headline per line.
- No extra commentary, no JSON, no markdown code blocks.

HEADLINE BEST PRACTICES:
- Google will mix and match your headlines. Every headline must make sense on its own and when paired with any description.
- Vary the angle across the {{ count }} headlines. Aim for a mix of:
  - The core hook/feature
  - A concrete benefit or outcome
  - A trust or proof point
  - A friction-removal or "how it works" line
  - A local-language or Hinglish variant if it fits the audience
  - A soft CTA or next-step action
- Do not write {{ count }} versions of the same sentence. Each headline should add a distinct idea.
- Avoid vague hype. Replace "Best trading tool" with specifics like "Backtest Ideas Without Code".
- Headlines should be readable, not keyword-stuffed fragments.

Now write {{ count }} headlines:

1.
2.
...
{{ count }}.
